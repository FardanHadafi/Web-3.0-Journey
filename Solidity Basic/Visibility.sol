// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract Visibility {
    // Private - Can be called only inside the contract 
    // Internal - Can be called only inside the contract and child contract 
    // Public - Can be called inside the contract and outside the contract 
    // External - Can be called from outside the contract only
}